<Window
	x:Class="APG_FEA2D.Views.SupportDisplacementLoadView"
	xmlns="https://github.com/avaloniaui"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="using:APG_FEA2D.ViewModels"
	Title="SupportDisplacementLoadView"
	Width="150"
	Height="200"
	d:DesignHeight="450"
	d:DesignWidth="800"
	x:DataType="vm:SupportDisplacementLoadViewModel"
	WindowStartupLocation="CenterOwner"
	mc:Ignorable="d">
	<StackPanel>
		<Label>Ux</Label>
		<TextBox Text="{Binding Ux}">Ux</TextBox>
		<Label>Uy</Label>
		<TextBox Text="{Binding Uy}">Uy</TextBox>
		<Label>Rz</Label>
		<TextBox Text="{Binding Rz}">Rz</TextBox>
		<Button x:DataType="vm:SupportDisplacementLoadViewModel" Command="{Binding SupportDiplacementLoadOkCommand}">Ok</Button>
	</StackPanel>
</Window>
